package com.slangword.domain;

public enum QuizMode {
    /** Show a definition, guess the slang word. */
    WORD_FROM_DEFINITION,
    /** Show a slang word, guess its definition. */
    DEFINITION_FROM_WORD
}
